# Production Environment Variables for VMware Deployment
# Copy this file to .env on your VMware host: /opt/erp-crm-app/.env

# JWT Secret - CHANGE THIS IN PRODUCTION!
JWT_SECRET=change_this_to_a_secure_random_string

# Node Environment
NODE_ENV=production

# Database Configuration
DATABASE=mongodb://mongo:27017/idurar-erp-crm

# Backend Port
PORT=8888

# Node Options (if needed for legacy dependencies)
NODE_OPTIONS=--openssl-legacy-provider

# Frontend API URL (adjust based on your VMware host IP/domain)
REACT_APP_API_URL=http://your-vmware-host-ip:8888/api

# MongoDB Configuration (optional, for advanced setup)
# MONGO_INITDB_ROOT_USERNAME=admin
# MONGO_INITDB_ROOT_PASSWORD=change_this_password
# MONGO_INITDB_DATABASE=idurar-erp-crm